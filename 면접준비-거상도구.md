# 거상 이미지 도구 — 면접 준비 노트

> 이 문서는 **외워서 읊는 대본이 아닙니다.** 실제 코드(`map_monsters.py`, `apply_corrections.py`, `make_aliases.py`)에서 네가 내린 결정을 "말로 설명"할 수 있게 정리한 것. 답변 뼈대는 참고만 하고, 반드시 네 말투·네 이해로 다시 풀어서 연습해.

---

## ⚠️ 먼저: 면접 전에 꼭 정리할 불일치 2개

면접관이 코드를 열어볼 수 있다. README/설명과 실제 코드가 다르면 신뢰가 깨진다. 아래 두 가지를 먼저 정리해.

**1. "아이템" vs "몬스터"**
포트폴리오 카드/README에는 "아이템 1,577종"이라고 썼는데, 지금 이 코드(`map_monsters.py`)는 **몬스터 도감**을 다룬다(`monster_id`, `monsters.csv`). 아이템용 도구가 따로 있는 거면 둘을 구분해서 설명하고, 같은 거라면 용어를 하나로 통일해라. "아이템 도구인데 코드는 monster네요?"라는 질문에 버벅이면 마이너스.

**2. 배경 제거 방식 — "flood fill" vs "배경판 차이"**
설명 글에는 "가장자리에서 연결된 어두운 픽셀만 flood fill로 투명 처리"라고 썼는데, 실제 `cutout()` 코드는 **배경판(bg plate)을 추정해 그 차이(diff)로 알파를 만드는 방식**이다. 둘은 다른 알고리즘이야. 아마 초기 버전이 flood fill이었고 지금 버전이 배경판 방식으로 진화한 듯. 면접에선 **최종 코드 기준(배경판 diff)**으로 설명하고, "처음엔 flood fill로 했다가 얼룩 문제로 배경판 추정 방식으로 바꿨다"는 식으로 **진화 과정**을 얘기하면 오히려 강점이 된다.

---

## 30초 프로젝트 요약 (엘리베이터 피치)

> "게임 도감 몬스터 이미지 약 1,600종을 거래/도감 사이트에 쓰려고 손으로 편집하려니 비현실적이었습니다. 그래서 **게임에 입력을 전혀 주지 않고**(스크린샷 파일만 감시) 캡처 → 배경 제거 → 이름 인식 → DB의 고유 id로 매칭 → `{id}.png` 저장까지 자동화하는 파이썬 반자동 파이프라인을 만들었습니다. 핵심은 OCR을 100% 신뢰하지 않고 **애매한 것만 사람이 1초 확인하는 human-in-the-loop 구조**로, 정확도와 속도를 둘 다 잡은 겁니다."

---

## 코드 이해 체크리스트 (이걸 다 설명할 수 있으면 합격 수준)

각 항목을 "이게 뭘 하고, 왜 이렇게 했는지" 한 문장으로 말할 수 있는지 스스로 점검해.

- [ ] `build_plate()` — 도감 배경판을 어떻게 추정하나
- [ ] `cutout()` — 배경판 diff로 누끼(알파) 만드는 흐름
- [ ] `binarize_title()` + `ocr_title()` — 이름/속성 OCR 전략
- [ ] `elem_by_color()` — OCR 실패 시 색으로 속성 판별
- [ ] `to_jamo()` + `name_score()` — 자모 분해 후 유사도 매칭
- [ ] `best_match()` — 자동확정 vs review vs ambiguous 결정 로직
- [ ] id 기반 저장 + 동일 이름 여러 id 처리
- [ ] `aliases` 별칭 학습 — 검수 결과를 다음 실행에 반영
- [ ] `review.html` — 사람이 검수하는 UI

---

## 예상 질문 + 답변 뼈대

### A. 설계 / 의사결정

**Q. 왜 완전 자동이 아니라 반자동(사람 개입)으로 만들었나요?**
- OCR·색상 판별이 100% 정확할 수 없다. 완전 자동으로 밀면 오매칭이 조용히 섞여 들어가 DB가 오염된다.
- 그래서 **신뢰도 높은 건 자동 확정하고(AUTO_MIN 0.85, 2위와 격차 0.04 이상), 애매한 것만 사람에게**. 대부분은 자동, 소수만 1초 확인 → 정확도와 속도 둘 다 확보.
- 배운 점: "완벽한 자동 인식을 고집하는 것보다 사람이 잠깐 확인하는 설계가 더 실용적"이라는 판단.

**Q. 게임에 매크로를 쓴 건가요? (안전/약관 관련)**
- 게임에 **어떤 입력도 주입하지 않는다.** 게임 자체 스크린샷 기능(F12)이 저장한 파일을 `watchdog`으로 감시해 **사후 처리만** 한다.
- 즉 "매크로 언어가 뭐냐"가 아니라 "**시스템이 게임에 입력을 넣느냐**"가 안전의 기준이라고 봤다. 이 도구는 입력을 안 넣으므로 계정 조작 리스크가 없다.

### B. 핵심 알고리즘 (여기서 깊게 물어봄)

**Q. 배경을 어떻게 제거했나요?**
- 도감 배경이 **타일이 반복되는 고정 UI**라는 점을 이용했다.
- `build_plate()`에서 여러 스크린샷을 쌓아, 반복 주기(period)를 자동 검출하고 **타일 위치(phase)별 중앙값(median)**으로 깨끗한 배경판을 만든다. (단순 전체 median이 아니라, 위치별로 색이 다른 타일 패턴이라 phase별 median이 필요)
- `cutout()`에서 원본과 배경판의 **차이(diff)**로 알파를 만들고, morphology(opening/closing)로 노이즈 제거, 작은 조각(blob) 제거, 살짝 blur해서 경계를 부드럽게. 마지막에 `(img - bg*(1-a))/a`로 전경 색을 복원한다.
- (초기엔 flood fill로 했다가 얼룩 문제로 배경판 방식으로 개선했다 — 진화 스토리로 말하기)

**Q. 이름 인식(OCR)은 어떻게 했고, 왜 그대로 저장 안 했나요?**
- EasyOCR ko 리더로 이름을, ja 리더로 속성 한자(火雷風水)를 따로 읽는다. `allowlist`로 **DB에 실제 존재하는 글자만** 인식 후보로 제한해 오인식을 줄였다.
- OCR 결과를 그대로 파일명으로 쓰면 오탈자·띄어쓰기 때문에 DB 매칭이 깨진다. 그래서 OCR은 **후보를 좁히는 힌트로만** 쓰고, 최종은 DB의 **고유 id로 `{id}.png` 저장** → 이름이 틀려도 id로 연결되니 DB 무결성이 안 깨진다.

**Q. 이름 매칭은 정확히 어떻게 하나요?**
- 한글을 **자모 단위로 분해**(`to_jamo`)한 뒤 `difflib`의 SequenceMatcher로 유사도를 잰다. 자모로 쪼개면 "받침 하나 틀림" 같은 오차에 강해진다.
- OCR 이름 뒤에 붙은 쓰레기 토큰을 하나씩 떼어낸 여러 후보(`make_variants`)를 만들어 비교하되, 토큰을 버릴수록 미세 감점 → **완전 일치가 항상 우선**.

**Q. OCR로 이름은 맞는데 속성(火/水 등)만 애매하면?**
- 동명이지만 속성만 다른 몬스터(색놀이 변형)가 있다. 한자 인식이 실패하면 **누끼된 몬스터 픽셀의 색상 분포(HSV hue)**로 속성을 추정한다(`elem_by_color`): 火=적, 雷=황, 風=녹, 水=청.
- 단, **후보 속성끼리만** 비교하고 1등이 2등의 1.5배 이상 우세할 때만 채택. 애매하면 `ambiguous` 처리해 사람에게 넘긴다. (무채색 스프라이트는 아예 `COLOR_SKIP`으로 제외)

**Q. 같은 이미지를 여러 번 저장해야 하는 경우(강화/중복)는?**
- +5/+10 강화는 기본과 이미지가 같다. 개별 캡처 대신 **이름 그룹으로 묶어** 매핑 단계에서 같은 이미지를 여러 id에 전파. 그래서 캡처 횟수를 줄였다.
- 코드상 DB의 동일 (이름,속성) 키에 여러 id가 있으면 **그 id들 전부에 저장**한다.

### C. 정확도 / 안정성

**Q. 자동 확정이 틀리면요? 어떻게 걸러내나요?**
- 임계값 미달(AUTO_MIN/AUTO_GAP)은 자동 확정하지 않고 `review`로 보낸다.
- 색상으로 확정한 id가 **한 번 실행에서 두 번 매칭되면** 색 오판 신호로 보고 `review`로 강등한다(중복 방지).
- 확정 실패건은 `review.html`(브라우저 UI)에서 사람이 후보 중 선택 → `corrections.csv`로 내보내 `apply_corrections.py`가 파일을 `{id}.png`로 정리한다.

**Q. 같은 실수를 매번 사람이 고쳐야 하나요?**
- 아니다. 검수에서 확정한 "OCR 오독 → id" 대응을 **별칭(aliases.csv)으로 학습**해서, 다음 실행부터는 같은 오독을 자동 확정한다. 일종의 가벼운 human-in-the-loop 학습.

### D. 한계 / 개선점 (반드시 준비 — 자주 물어봄)

솔직하게 인정하되 "이렇게 개선하겠다"까지 말하면 좋다.
- **자동화된 테스트가 없다.** 정답 라벨셋을 만들어 매칭 정확도를 수치로 측정하고 회귀 테스트를 붙이고 싶다.
- **하드코딩된 좌표(ROI, 1024x768 고정).** 해상도가 바뀌면 깨진다. 앵커 이미지 기반으로 좌표를 자동 탐지하면 견고해진다.
- **파라미터가 경험적(임계값 T0/T1, 0.85 등).** 데이터가 늘면 재튜닝 필요.
- 색상 판별이 스프라이트 색에 의존 → 예외를 `COLOR_SKIP`로 수동 관리 중.

### E. 행동 질문

**Q. 가장 어려웠던 문제는?**
→ 이름 매칭. OCR을 그대로 믿으면 DB가 깨진다는 걸 깨닫고, "OCR을 정답이 아니라 힌트로"로 **접근 자체를 바꾼** 게 가장 큰 전환점이었다고.

**Q. 가장 자랑스러운 결정은?**
→ "완전 자동"의 함정을 인정하고 반자동+별칭 학습으로 간 것. 그리고 안전을 언어가 아니라 "입력 주입 여부"로 판단한 것.

---

## 연습 방법

1. 위 체크리스트 9개를 **아무것도 안 보고** 소리 내어 설명해본다.
2. 막히는 항목은 해당 함수 코드를 다시 읽고, 주석을 네 말로 바꿔 적어본다.
3. B(핵심 알고리즘)는 화이트보드에 그림으로 그리며 설명하는 연습. 특히 배경판 추정과 매칭 로직.
4. D(한계)는 반드시 1~2개 준비. "약점 없다"가 제일 나쁜 답.
